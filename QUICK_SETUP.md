# Быстрая настройка продакшн окружения

## Одна команда для всего!

Выполните на сервере:

```bash
cd /var/www/xk-media-backend
git pull
chmod +x scripts/setup_production.sh
sudo ./scripts/setup_production.sh
```

Скрипт автоматически:
1. ✅ Обновит код из репозитория
2. ✅ Настроит SSL сертификат (Let's Encrypt)
3. ✅ Сменит пароль администратора (запросит у вас)
4. ✅ Настроит резервное копирование (ежедневно в 2:00)
5. ✅ Настроит мониторинг (каждые 5 минут)
6. ✅ Перезапустит все сервисы

## Что нужно будет указать:

1. **Домен** (по умолчанию: `xk-media.ru`)
2. **Email для уведомлений** (по умолчанию: `admin@xk-media.ru`)
3. **Новый пароль администратора** (минимум 8 символов)

## После выполнения:

- 🌐 Сайт будет доступен по HTTPS: `https://xk-media.ru`
- 👤 Админ панель: `https://xk-media.ru/admin`
- 📝 Логи мониторинга: `/var/log/xk-media-monitor.log`
- 💾 Бэкапы: `/var/backups/xk-media/`

## Проверка работы:

```bash
# Статус сервисов
systemctl status xk-media
systemctl status nginx

# Логи мониторинга
tail -f /var/log/xk-media-monitor.log

# Cron задачи
crontab -l
```

---

**Готово!** Все настройки применены автоматически. 🎉
