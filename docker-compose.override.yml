version: '3.8'

services:
  web:
    env_file:
      - .env.local
    ports:
      - "8009:8080"
    environment:
      - BASE_URL=${BASE_URL:-http://localhost:8009}
  db:
    # avoid clashing with any local Postgres
    ports:
      - "5439:5432"
